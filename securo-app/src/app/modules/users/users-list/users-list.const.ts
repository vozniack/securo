import { ListEntry } from '../../../shared/components/list/list.interface';

export const userListEntry: ListEntry = {
  icon: 'person',
  firstLine: ['firstName', 'lastName'],
  secondLine: ['email']
};
